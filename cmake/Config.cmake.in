#################################
## Configuration template file ##
#################################
include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Poco 1.4.3 REQUIRED COMPONENTS Foundation Net Util XML)

# Our library dependencies (contains definitions for IMPORTED targets).
include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")

# Set legacy "_LIBRARIES" variable (e.g. due to that ament_cmake seems to need it),
# however it should not be required with modern CMake (i.e. targets and properties).
set(@PROJECT_NAME@_LIBRARIES @PROJECT_NAME@::@PROJECT_NAME@)
